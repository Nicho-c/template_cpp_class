#ifndef {{CLASS_NAME_U}}_H
# define {{CLASS_NAME_U}}_H
	#include <iostream>
	#include <string>

	class {{CLASS_NAME}} {
		private :
			int			id;
			std::string	key;
			static int	_nb{{CLASS_NAME}};

		public :
		{{CLASS_NAME}} (void);
			{{CLASS_NAME}} (const std::string&);
			{{CLASS_NAME}} (const {{CLASS_NAME}}&);
			~{{CLASS_NAME}} (void);
			{{CLASS_NAME}}& operator=(const {{CLASS_NAME}}&);
	
	};
#endif