#ifndef {{CLASS_NAME_U}}_H
# define {{CLASS_NAME_U}}_H
	#include "headers.hpp"

	class {{CLASS_NAME}} {
		private :
			int			id;
			std::string text;

		public :
			{{CLASS_NAME}} (void);
			{{CLASS_NAME}} (const std::string&);
			{{CLASS_NAME}} (const {{CLASS_NAME}}&);
			~{{CLASS_NAME}} (void);
			{{CLASS_NAME}}& operator=({{CLASS_NAME}}&&) noexcept;
		
		int		get_id();		
		void	set_text(std::string);
	};
#endif {{CLASS_NAME_U}}_H