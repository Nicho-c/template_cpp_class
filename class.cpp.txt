#include "{{CLASS_NAME}}.hpp"

{{CLASS_NAME}}::{{CLASS_NAME}} (void){
	this->id = {{CLASS_NAME}}::_nb{{CLASS_NAME}}++;
}

{{CLASS_NAME}}::{{CLASS_NAME}} (const std::string& str){
	this->id = {{CLASS_NAME}}::_nb{{CLASS_NAME}}++;
	this->key = str;
}

{{CLASS_NAME}}::{{CLASS_NAME}} (const Contact& other)
{
	this->id = {{CLASS_NAME}}::_nb{{CLASS_NAME}}++;
	this->key = other.key;
}

{{CLASS_NAME}}& {{CLASS_NAME}}::operator=(const {{CLASS_NAME}} & other)
{
	if (this == &other) {
		return *this;
	}
	this->id = {{CLASS_NAME}}::_nb{{CLASS_NAME}}++;
	this->key = other.key;
	return (*this);
}

{{CLASS_NAME}}::~{{CLASS_NAME}} (void)
{
	std::cout << "Suppresion du {{CLASS_NAME}}" << std::endl;
}
